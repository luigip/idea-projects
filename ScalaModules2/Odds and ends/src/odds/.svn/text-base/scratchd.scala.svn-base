package odds

object scratchd extends App {

  def find(M: Array[Array[Int]], W: Int): Option[(Int, Int, Int)] = {
    for {
      x <- M.indices.reverse
      y <- M(x).indices.reverse
      a = M(x)(y)
      if 0 < a && a <= W
    } return Some(x, y, a)
    None
  }
}