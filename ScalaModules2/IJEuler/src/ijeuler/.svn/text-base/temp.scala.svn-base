package ijeuler


class DoubleWrapper(value: Double) {
  override def equals(o: Any): Boolean = {
    if (o.isInstanceOf[{def toDouble}])
      value == o.asInstanceOf[{def toDouble}].toDouble
    else false
  }
  override def hashCode(): Int = value.##
}
